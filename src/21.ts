using System.Security.Cryptography;
using System.Text;
using System.Linq;
using System.Collections.Generic;
using System.Diagnostics;
using System.Numerics;
using System;

class MathExercises {
    static void Main() {
        // Example usage: Adding two numbers
        int a = 5, b = 3;
        Console.WriteLine($"The sum of {a} and {b} is: {a + b}");
        
        // Example usage: Multiplying two numbers
        int c = 6, d = 4;
        Console.WriteLine($"The product of {c} and {d} is: {c * d}");
    }
}
